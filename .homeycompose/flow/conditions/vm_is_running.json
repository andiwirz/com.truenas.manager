{
  "id": "vm_is_running",
  "title": {
    "en": "Virtual machine !{{is|is not}} running",
    "de": "VM !{{läuft|läuft nicht}}"
  },
  "args": [
    {
      "name": "device",
      "type": "device",
      "filter": "driver_id=truenas-vm"
    }
  ]
}
